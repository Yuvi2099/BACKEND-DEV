EXERCISE 1

touch README.md
git add README.md
git commit -m "[YUVRAJ]:added the readme file for git and github"

echo "this is a readme file" >> README.md
git add README.md
git commit -m "Update README content"
git push origin main

----------------------------------------------------------------------------------------------------------------------------------

EXERCISE 2

git checkout -b feature
touch hello.txt
echo "Hello World , how was your day !!!" >> README.md
git add hello.txt
git commit -m "Add hello file"
git checkout main
git merge feature
git branch -d feature
git push origin main

------------------------------------------------------------------------------------------------------------------------------------

EXERCISE 3

git checkout -b X
git checkout main
git checkout -b Y
git checkout X
echo "the weather today is clear and sunny" > hello.txt
git add hello.txt
git commit -m "[YUVRAJ]:modified hello file"
git checkout Y
echo \n"       " > hello.txt
git add conflict.txt
git commit -m "[YUVRAJ]:modified hello file"
git checkout main
git merge X
git merge Y
# Open hello.txt and fix the conflict
git add hello.txt
git commit -m "Resolved merge conflict"

--------------------------------------------------------------------------------------------------------------------------------

EXERCISE 4

git clone (github repo link)
cd repo-name
git checkout -b my-feature
git add .
git commit -m "Improve feature"
git push origin my-feature

------------------------------------------------------------------------------------------------------------------------------------

EXERCISE 5

git init
touch app.js index.html style.css
git add .
git commit -m "Initial project setup"
touch .gitignore
echo "node_modules/" >> .gitignore
git add .gitignore
git commit -m "Add gitignore file"
git stash
git remote add origin (repo-link)
git push origin main
git push origin --tags

-----------------------------------------------------------------------------------------------------------------------------------

